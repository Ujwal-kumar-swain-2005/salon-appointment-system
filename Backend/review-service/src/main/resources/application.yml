spring:
  application:
    name: review-service

  datasource:
    url: jdbc:mysql://localhost:3306/review_db?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC
    username: root
    password: Ujwal@2003
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect

server:
  port: 8089

eureka:
  instance:
    hostname: localhost
    prefer-ip-address: false
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8070/eureka/


logging:
  level:
    com.example.review_service.feignclient.UserFeignClient: DEBUG

feign:
  client:
    config:
      USER-SERVICE:
        loggerLevel: FULL